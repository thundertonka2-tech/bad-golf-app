# Keys that LOOK like round fields to guard_merge_registry.py but are not stored
# on the round, so they need no merge rule. One per line, with the reason.
# Anything not listed here and not in BG_MERGE_RULES blocks the commit.
_moneyArchiveError     # transient diagnostic set on the in-memory round when an archive throws; never persisted
ctp                    # _g there is a GAMES map (league week -> games), not a round; ctp is a game name
longPutt               # same — a game name inside g.games, not a top-level round key
onclick                # DOM handler assignment on an element that happens to be named `g`
